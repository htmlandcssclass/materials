<!doctype html>
<html>
  <head>
    <title><%= title || "HTML and CSS Class" %></title>

    <%= stylesheet_link_tag 'global', 'navigator' %>
    <%= yield_content :head %>
  </head>

  <body class="<%= page_classes %> navigator">
    <%= partial 'layouts/header' %>
    <div class="content">
      <% unless title.blank? %><h1><%= title %></h1><% end %>
      <%= yield %>
    </div>
  </body>
</html>