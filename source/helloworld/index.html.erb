<section>
<%= partial 'shared/title' %>
<h2>Let's make a little web page.</h2>
<a class="preview" href="/helloworld/modernist/"><img src="/images/helloworld/modernist.png" /></a>
</section>


<section>
  <h3>Set up</h3>
  <ul>
    <li>Create a new workspace named <code>hello_world</code> on <a ref="http://c9.io">Cloud9</a>.</li>
  </ul>
</section>

<section>
  <h3>HTML</h3>
  <ul>
    <li>Write a headline using the <code>h1</code> tag name.</li>
    <li>Write two short paragraphs using the <code>p</code> tag name.</li>
  </ul>
</section>

<section>
  <h3>CSS</h3>
  <p>Link your stylesheet to your HTML file by adding a <code>link</code> tag.</p>
  <pre><code>&lt;link rel=&quot;stylesheet&quot; href=&quot;modernist_style.css&quot;&gt;</code></pre>
</section>

<section>
  <p>Control layout using <code>margin</code> and <code>padding</code>.</p>
  <p>Control typography using <code>color</code>, <code>font-family</code>, <code>font-size</code>, <code>font-weight</code>, and <code>line-height</code>.</p>
  <p>Control decoration using <code>background</code> and <code>border</code>.</p>
</section>

<%= partial 'shared/colors', locals: {colors: data.colors.helloworld_modernist} %>
